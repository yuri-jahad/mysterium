{
  "name": "code-breakers-backend",
  "module": "app.ts",
  "types": "module",
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=.env.local --watch app.ts",
    "start": "bun --env-file=.env.prod --watch app.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "ngrok": "^5.0.0-beta.2"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "code-breakers-backend": ".",
    "jose": "^5.9.6"
  }
}